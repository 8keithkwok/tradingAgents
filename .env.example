# Copy to .env at project root. Used by docker compose and by backend when run locally.
# Local dev: use .env. UAT/PROD on same machine: use .env.uat and .env.prod with different ports and DB names.

# Postgres (for db service and DATABASE_URL substitution)
POSTGRES_USER=ta_backend
POSTGRES_PASSWORD=PvcbGk5Jw2NJj7cp
POSTGRES_DB=trading_agents

# Host ports for Docker (optional; defaults: 5432, 8000, 3000)
PORT_DB=5432
PORT_BACKEND=8000
PORT_FRONTEND=3000

# Backend: DB URL (Docker uses host "db"; local dev use "localhost")
DATABASE_URL=postgresql+asyncpg://postgres:changeme@localhost:5432/trading_agents

# Backend: API keys (optional)
# OPENAI_API_KEY=sk-...
# DEEPSEEK_API_KEY=...
